import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    // flex: 1,
  },
  label: {
    fontSize: 14,
    marginBottom: 8,
    fontFamily: 'Inter',
    fontWeight: '500'
  },
  input: {
    paddingLeft: 8,
    paddingRight: 8,
    paddingTop: 8,
    paddingBottom: 8,
    borderRadius: 4,
    borderWidth: 1
  }
})

export default styles
